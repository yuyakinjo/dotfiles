{
  "name": "my",
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^5.9.3"
  },
  "bin": {
    "my": "dist/index.js"
  },
  "private": true,
  "scripts": {
    "build": "decopin build",
    "dev": "decopin dev",
    "typecheck": "tsc --noEmit",
    "link": "bun run build && mkdir -p ~/.local/bin ~/.zsh/completions && ln -sf \"$PWD/dist/index.js\" ~/.local/bin/my && cp dist/completions/_my ~/.zsh/completions/_my"
  },
  "type": "module",
  "dependencies": {
    "decopin-cli": "^2026.903.717"
  }
}
